"""House IAP runtime — same call order as MMVFIRM/iap-v1.0, different world.
See artifacts/house_iap_runtime.py in the working sandbox for the verified copy if this blob is truncated.
"""
from house_iap_runtime import *  # local module in this folder when copied whole
